## Repository environments

The repository has the followind environments configured within the GH Repo Envs:
* `DEV`
* `CANARY`
* `PROD`

These environments have the same secret keys with different values.
